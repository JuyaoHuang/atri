# ============================================================
# FastAPI Server Configuration
# FastAPI 服务器配置
#
# Reference: docs/Phase5_执行规格.md §三
# ============================================================

host: "0.0.0.0"
port: 8430

cors:
  allow_origins: ["*"]
  allow_methods: ["*"]
  allow_credentials: true

websocket:
  heartbeat_interval: 20
  heartbeat_timeout: 60
